<!DOCTYPE html>
<html>
<head>
    <title>Home</title>
</head>
<body>
    <h1>All URLs</h1>
    <ul>
        <% urls.forEach(url => { %>
            <li><%= url.shortId %>: <%= url.redirectURL %></li>
        <% }) %>
    </ul>
</body>
</html>
